import SwiftUI

/// The running who-owes-whom summary shown at the top of the Bills home screen.
///
/// Task 10.7 ships this as an empty placeholder so the home layout has its slot; Task 10.8 fills it
/// in with the balance rows and the "which one is you?" first-run picker.
struct BalancesSummary: View {
    var body: some View {
        EmptyView()
    }
}
