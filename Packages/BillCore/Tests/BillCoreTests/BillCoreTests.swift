import Testing
@testable import BillCore

// Placeholder proving the package + Swift Testing target build and run via `swift test`.
// The real invariant suites (allocate conservation, the $97.20 acceptance bill) land in EPIC 02.
@Test("BillCore package builds")
func packageBuilds() {
    #expect(Bool(true))
}
